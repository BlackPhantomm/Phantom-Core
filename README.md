# Phantom Core 💠

**The Official Profile Generator for the Phantom Ecosystem.**

> *Status: Private Development (Pre-Alpha)*

## ⚡ Installation
1. Clone the repo.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

## 🚀 Usage
Run the source code directly:
```bash
python phantom_gui.py

📦 Building the .exe
To compile a standalone executable:

pyinstaller --noconsole --onefile --name PhantomCore phantom_gui.py

